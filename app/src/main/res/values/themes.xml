<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ThoughtfocusApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/green</item>
    </style>

    <style name="Theme.ThoughtfocusApplication" parent="Base.Theme.ThoughtfocusApplication" />

    <style name="Theme.MainActivity" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize attributes for the new theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimary</item>
        <!-- Add more customizations for AnotherTheme -->
    </style>

</resources>